import React from 'react'

const SectionTitle = ({title}) => {
  return (
		<div className='mx-8 mt-16 text-center text-white text-3xl sm:text-5xl font-bold'>
			<h1>{title}</h1>
		</div>
	);
}

export default SectionTitle
